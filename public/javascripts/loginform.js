document.addEventListener("DOMContentLoaded",()=>{
    console.log("login.js is loaded up")
    let loginform = document.getElementById("login")
    loginform.addEventListener("submit",(event)=>{
        // event.preventDefault()
        })
})